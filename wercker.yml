box: python
build:
  steps:
deploy:
  steps:
    - hotakasaito/elastic-beanstalk-deploy@1.2.3:
        key: $ACCESS_KEY
        secret: $ACCESS_SECRET
        app_name: $APP_NAME
        env_name: $ENV_NAME
        region: ap-northeast-1